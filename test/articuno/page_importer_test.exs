defmodule Articuno.PageImporterTest do
  use ExUnit.Case
  # doctest Articuno.PageImporter

  # alias Articuno.PageImporter
  # alias Articuno.Job

  test "can generate pages" do
    # Create a file system rep of a site with a few pages
    # Create a job pointing at that folder

    # {:ok, dir_path} = Temp.mkdir()
    # build_path = dir_path

    # job = %Job{
    #   site_info: site_info,
    #   site_path: site_path,
    #   build_path: build_path,
    #   root_folder: root_folder
    # }
    # Pass the job to the page importer

    # verify it has generated the expect file tree
  end
end
